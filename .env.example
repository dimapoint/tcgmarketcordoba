# Este archivo se bundlea como asset de Flutter: SOLO valores públicos.
API_URL=http://localhost:8080

# OAuth client ID de Google (el mismo que GOOGLE_CLIENT_ID del backend).
# Es un valor público. Si falta, el botón "Continuar con Google" no se muestra.
GOOGLE_CLIENT_ID=xxxx.apps.googleusercontent.com
